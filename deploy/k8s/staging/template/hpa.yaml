apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: gau-phim-service-hpa
  namespace: bao-${DEPLOY_ENV}-env
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: gau-phim-frontend-deployment
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80

