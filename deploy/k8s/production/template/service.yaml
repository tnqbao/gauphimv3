apiVersion: v1
kind: Service
metadata:
  name: gau-phim-service
  namespace: bao-${DEPLOY_ENV}-env
spec:
  selector:
    app: gau-phim-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: bao-${DEPLOY_ENV}-env
spec:
  selector:
    app: redis-phim-service
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
---
